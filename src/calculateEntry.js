const data = require('../data/zoo_data');

const countEntrants = (entrants) => {
  // seu código aqui - testando
};

const calculateEntry = (entrants) => {
  // seu código aqui
};

module.exports = { calculateEntry, countEntrants };
